apiVersion: v1
kind: Secret
metadata:
  name: openrouter-api-key
  namespace: ai-bot
type: Opaque
# Create via CLI on VPS (do NOT commit actual values):
#   kubectl -n ai-bot create secret generic openrouter-api-key \
#     --from-literal=OPENROUTER_API_KEY=sk-or-xxxxx
