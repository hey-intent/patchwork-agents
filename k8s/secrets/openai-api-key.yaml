apiVersion: v1
kind: Secret
metadata:
  name: openai-api-key
  namespace: ai-bot
type: Opaque
# Create via CLI on VPS (do NOT commit actual values):
#   kubectl -n ai-bot create secret generic openai-api-key \
#     --from-literal=OPENAI_API_KEY=sk-xxxxx
