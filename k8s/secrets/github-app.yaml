apiVersion: v1
kind: Secret
metadata:
  name: github-app
  namespace: ai-bot
type: Opaque
# Create via CLI on VPS (do NOT commit actual values):
#   kubectl -n ai-bot create secret generic github-app \
#     --from-literal=GITHUB_APP_ID=<numeric-app-id> \
#     --from-file=GITHUB_PRIVATE_KEY=/etc/secrets/github-app.pem
