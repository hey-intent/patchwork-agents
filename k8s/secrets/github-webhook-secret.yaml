apiVersion: v1
kind: Secret
metadata:
  name: github-webhook-secret
  namespace: ai-bot
type: Opaque
# Create via CLI on VPS (do NOT commit actual values):
#   kubectl -n ai-bot create secret generic github-webhook-secret \
#     --from-literal=WEBHOOK_SECRET=<your-webhook-secret>
